// const Sync = require('../../src/index')

describe('sync geojson', () => {
  describe('new instance', () => {
    it('normal', () => {

      // const sync = new Sync({keyword: '山东省', dir: './test/geojson'})
    })
  })
})